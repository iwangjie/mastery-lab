# 本关目标

今天只做一件事：让 shell 脚本安全处理命令参数。

请修改：

```text
workspace/bin/quote_args.sh
```

让它把收到的每一个参数原样逐行输出。重点是带空格、多个空格、星号这类容易被 shell 拆分或展开的参数。

完成后回到项目根目录运行：

```bash
./mastery check
```

不要先写复杂脚本。先证明自己理解 `"$@"` 和未引用 `$@` 的区别。
